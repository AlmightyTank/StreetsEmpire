**/bin/
**/obj/
**/node_modules/
**/dist/
*.tsbuildinfo
.vs/
.idea/
.env

# Database dumps. Every one of them is a complete copy of every account, address and hashed password
# in the game, so committing one would be worse than committing the .env above.
/backups/
*.dump
*.dump.partial
*.user
*.suo
/.pnpm-store
/.tmp-build/
docker-compose.override.yml
